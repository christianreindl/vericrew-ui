import {
  Meta,
  Canvas,
  Story,
  Props,
} from "@storybook/addon-docs/blocks";

<Meta
  title="Components/Button"
  argTypes={{
    label: { control: 'text' },
    size: { control: 'boolean' },
  }}
/>

# Welcome to Storybook

Storybook helps you build UI components in isolation from your app's business logic, data, and context.
That makes it easy to develop hard-to-reach states. Save these UI states as **stories** to revisit during development, testing, or QA.

Browse example stories now by navigating to them in the sidebar.
View their code in the `src/stories` directory to learn how they work.
We recommend building UIs with a [**component-driven**](https://componentdriven.org) process starting with atomic components and ending with pages.

<div className="tip-wrapper">
  <span className="tip">Tip</span>Edit the Markdown in{" "}
  <code>src/stories/Introduction.stories.mdx</code>
</div>

export const Template = ({ label, primary, ...args }) => {
  return `<div style="color:${primary ? "red" : "blue"}">${label}</div>`;
};

<Canvas>
  <Story name="Unchecked" args={{primary: true, label: "Red Text"}}>
    {Template.bind({})}
  </Story>
  <Story name="Checked" args={{primar: false, label: "Blue Text"}}>
    {Template.bind({})}
  </Story>
</Canvas>


<Props story="Unchecked" />
